<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Slide" parent="@style/AppTheme">
        <!--Required-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">
            @style/AppTheme.Slide.Animation
        </item>
    </style>

    <style name="AppTheme.Slide.Animation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/act_slide_in
        </item>
        <item name="android:activityOpenExitAnimation">@anim/act_slide_out
        </item>
        <item name="android:activityCloseEnterAnimation">@anim/act_slide_in
        </item>
        <item name="android:activityCloseExitAnimation">@anim/act_slide_out
        </item>
    </style>

    <style name="AppTheme.AppBarOverlay"
           parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!--spinner的样式-->
    <style name="style_spinner">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:dropDownVerticalOffset">40dp</item>
        <item name="android:dropDownWidth">1000dp</item>
        <item name="android:layout_margin">0dp</item>

    </style>

    <!--定义spinner之间的线的样式-->
    <style name="style_mian_verline">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">25dp</item>
        <item name="android:background">#dcdcdc</item>
        <item name="android:layout_gravity">center_vertical</item>
    </style>

    <style name="AnimTools" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_in</item>
        <item name="android:windowExitAnimation">@anim/push_out</item>
    </style>

    <style name="CollapsingText">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">italic</item>
        <item name="android:textColor">#0058f1</item>
    </style>

    <style name="ExtendText">
        <item name="android:textSize">40sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#fff</item>
    </style>

    <declare-styleable name="CardStackView">
        <attr name="stackOverlapGaps" format="dimension"/>
        <attr name="stackOverlapGapsCollapse" format="dimension"/>
        <attr name="stackHeaderHeight" format="dimension"/>
        <attr name="stackDuration" format="integer"/>
        <attr name="stackNumBottomShow" format="integer"/>
        <attr name="stackAnimationType">
            <flag name="allDown" value="0"/>
            <flag name="upDown" value="1"/>
            <flag name="upDownStack" value="2"/>
        </attr>
    </declare-styleable>

    <!-- 输入框 -->
    <style name="editText">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textSize">19dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <!-- 设置选项一条的主文本 -->
    <style name="setItemTextFirst">
        <item name="android:textSize">17dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="setItemTextSecon">
        <item name="android:textSize">17dp</item>
        <item name="android:textColor">@color/gray5</item>
        <item name="android:gravity">right|center_vertical</item>
    </style>

</resources>
